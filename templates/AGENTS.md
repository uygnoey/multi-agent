# AGENTS.md (generated by orchestrator)

This directory is the **target web service** built by the multi-agent orchestrator.
This file is auto-loaded by agents that read `AGENTS.md` (e.g. OpenAI Codex).
(Claude-family backends read `CLAUDE.md`; both files carry the same conventions.)

## Tech stack
{{STACK}}

## Directory layout
- `docs/design/` design · `docs/test/` test sheets · `backend/` · `frontend/` · `db/` · `tests/`
- `.orchestrator/` run state (board/events/results). **Do not edit**; excluded from VCS.

## Result-file coordination protocol (important)
- Edit only files inside cwd. Never access paths outside it.
- When done, write a result JSON to the assigned `.orchestrator/results/<role>__<unit>.json`:
  `{"status","artifacts":[],"notes":[],"blockers":[],"units":[]}`.
- The board (`.orchestrator/board.json`) is written only by the orchestrator.
- Do not touch other work units' files. Report shared needs via `blockers`.

## Coding conventions
- Small, clear changes; match existing style; secrets via env/secret store; runnable code only.

## Spec excerpt
{{SPEC_EXCERPT}}
